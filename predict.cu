void predict(int NumberDataPredict, int Nfeature, double* DataPredict, ){
    double value = 0;
    if(blockIdx.x *blockDim.x + threadIdx.x < NumberDataPredict){
        for(int feature = 0; feaute < Nfeature; ++feature){

        } 

    }
}